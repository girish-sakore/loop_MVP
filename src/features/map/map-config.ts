export const MAP_ZOOM_SCALE = 1.8;       // how zoomed the map is vs container width
export const FOCUS_X_FRACTION = 0.05;    // node sits 5% in from left of viewport
export const FOCUS_Y_FRACTION = 0.4;     // node sits 40% down from top of viewport